<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- MAPA -->
    <ImageView
        android:id="@+id/image_map"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="matrix"
        android:src="@drawable/mapa_hacknation_dark" />
    <com.example.gpsdeniedmapapp.TrajectoryOverlayView
        android:id="@+id/trajectory_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- PANEL METADATA / STATUSÓW (składany) -->
    <LinearLayout
        android:id="@+id/metadata_panel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:orientation="vertical"
        android:background="#66000000"
        android:padding="4dp">

        <!-- Header z chevronem -->
        <LinearLayout
            android:id="@+id/metadata_header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="4dp"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/image_metadata_chevron"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@android:drawable/arrow_down_float"
                android:rotation="0" />

            <TextView
                android:id="@+id/text_metadata_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:text="Status / debug" />
        </LinearLayout>

        <!-- To jest nasze dawne text_metadata: treść, którą będziemy zwijać/rozwijać -->
        <TextView
            android:id="@+id/text_metadata"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="4dp"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:visibility="gone"
            android:text="Metadata..." />
    </LinearLayout>

    <!-- Heading tekstowy i róża wiatrów – mogą zostać jak były -->
    <TextView
        android:id="@+id/text_heading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="#66000000"
        android:textColor="#FFFFFF"
        android:padding="8dp"
        android:textSize="14sp"
        android:layout_gravity="top|center"
        android:text="Heading: 0°" />

    <ImageView
        android:id="@+id/image_compass_rose"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_compass_rose"
        android:layout_gravity="top|end"
        android:layout_margin="8dp" />

    <!-- Dolny pasek: START + krótki status (możesz go później usunąć, jeśli wszystko przeniesiemy do debug panelu) -->
    <!-- Dolny pasek: START + logo na środku -->
    <RelativeLayout
        android:id="@+id/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:padding="8dp"
        android:background="#66000000">

        <Button
            android:id="@+id/button_start"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:text="START"
            android:background="#000000"
            android:textColor="#FFA500" />

        <ImageView
            android:id="@+id/image_logo"
            android:layout_width="248dp"
            android:layout_height="48dp"
            android:layout_centerInParent="true"
            android:src="@drawable/logo_fireops_h" />
        <!-- podmień @drawable/your_logo na nazwę Twojego logo, np. @drawable/forpet_logo -->
    </RelativeLayout>


</FrameLayout>

